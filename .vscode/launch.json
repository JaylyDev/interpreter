{
    "configurations": [
        {
            "name": "Launch",
            "cwd": "${workspaceFolder}/src/compile",
            "type": "node",
            "request": "attach",
            "preLaunchTask": "compiler", // The name of the task defined above
        }
    ]
}