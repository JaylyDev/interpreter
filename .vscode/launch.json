{
  "configurations": [
      {
        "name": "Launch",
        "cwd": "${workspaceFolder}/src/compile",
        "type": "node",
        "request": "attach",
        "preLaunchTask": "echotest", // The name of the task defined above
      }
  ]   
}